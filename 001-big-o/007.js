/**
 * Big O Rule 2: Remove Constants - Part 2
 */

{
  function compressBoxesTwice(boxes) {
    boxes.forEach(function (box) {
      console.log(box);
    });

    boxes.forEach(function (box) {
      console.log(box);
    });
  }
}

// => Time Complexity: O(2n) => O(n)
