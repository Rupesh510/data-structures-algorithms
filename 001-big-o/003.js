/**
 * O(1) => Constant Time
 */

{
  const myBoxes = [0, 1, 2, 3, 4, 5];

  function logFirstBox(boxes) {
    console.log(boxes[0]);
  }

  logFirstBox(myBoxes); // O(1) => Constant Time
}

// => O(1)
